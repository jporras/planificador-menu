_format_version: "3.0"
_transform: true

services:
  - name: planificador-backend
    url: http://backend:8080
    routes:
      - name: planificador-api
        paths:
          - /api
        strip_path: false
